# supabase/config.toml
# Minimal config for the AIS Edge Function.
# verify_jwt=false makes the function callable from the simple HTML prototype without user login.
# For production internal app, enable SSO/auth and change this later.

[functions.ais-latest]
verify_jwt = false
